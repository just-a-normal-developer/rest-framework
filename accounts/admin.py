from django.contrib import admin

# Register your models here.
#actually for having the access in model attributes you need to use decorator or if you are using the class based code you should use inheritance to register the admin on the model